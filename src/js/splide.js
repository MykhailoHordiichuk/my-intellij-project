// var splide = new Splide( '.splide', {
//   type   : 'loop',
//   padding: '5rem',
// } );

// splide.mount();





var splide = new Splide( '.splide', {
  perPage: 2,
  rewind : true,
} );

splide.mount();